// Placeholder shell — screens are implemented in Phase 4 against the imported
// design reference. Uses token utilities so the theme wiring is exercised.
export default function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-frame flex-col items-center justify-center gap-2 bg-surface text-ink">
      <h1 className="font-sans text-screen font-bold tracking-screen">INDEX</h1>
      <p className="font-mono text-label text-accent">PHASE 0 — SCAFFOLD</p>
    </main>
  )
}
